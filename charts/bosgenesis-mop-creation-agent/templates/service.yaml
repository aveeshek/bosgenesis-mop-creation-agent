apiVersion: v1
kind: Service
metadata:
  name: {{ include "bosgenesis-mop-creation-agent.fullname" . }}
  labels:
    {{- include "bosgenesis-mop-creation-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bosgenesis-mop-creation-agent.selectorLabels" . | nindent 4 }}

