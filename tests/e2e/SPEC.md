# End-to-End Tests Specification

## Intent

`tests/e2e/` will verify full MoP generation flows against controlled test evidence.

## Future scenarios

- Platform-only namespace reconstruction.
- Application-mode schema-only reconstruction.
- Codex refinement loop.
- Standalone REST trigger.

